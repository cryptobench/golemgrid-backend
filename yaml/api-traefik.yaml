# dashboard.yaml
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: api-ingress
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`192.168.1.98`)
      kind: Rule
      services:
        - name: api
          port: 8002
